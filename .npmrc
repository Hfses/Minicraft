# React Native autolinking resolves native modules via Node resolution; a hoisted
# node_modules layout is the most reliable for RN/Expo Android builds in a pnpm
# monorepo.
node-linker=hoisted
shamefully-hoist=true
